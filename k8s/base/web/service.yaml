apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: mikrouli
  labels:
    app.kubernetes.io/name: web
    app.kubernetes.io/part-of: mikrouli
spec:
  selector:
    app.kubernetes.io/name: web
  ports:
    - port: 80
      targetPort: 8080
      name: http
