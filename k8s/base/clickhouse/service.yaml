apiVersion: v1
kind: Service
metadata:
  name: clickhouse
  namespace: mikrouli
  labels:
    app.kubernetes.io/name: clickhouse
    app.kubernetes.io/part-of: mikrouli
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: clickhouse
  ports:
    - port: 8123
      targetPort: 8123
      name: http
    - port: 9000
      targetPort: 9000
      name: native
