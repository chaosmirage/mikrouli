apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: mikrouli
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: mikrouli
spec:
  selector:
    app.kubernetes.io/name: api
  ports:
    - port: 3000
      targetPort: 3000
      name: http
